// for the start button 
const startButton = document.querySelector('.started-btn');

// add an event listener for the "next button
startButton.addEventListener('click', () => {
  window.location.href = 'basecity.html'; // where the button goes 
});